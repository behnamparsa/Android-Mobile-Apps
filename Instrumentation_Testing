Here is the code to run in PowerShell scirpt to run the instrumentation test locally to fix the java version required and compatible with gradle 
and then run the connectedtst and generate the result report folder in build/report:

$env:JAVA_HOME = "C:\Program Files\Eclipse Adoptium\jdk-21.x.x.x-hotspot"
$env:Path = "$env:JAVA_HOME\bin;$env:Path"

java -version   # should show 17 or 21, NOT 25.0.1
./gradlew connectedAndroidTest

#running GMD

./gradlew pixel2Api32DebugAndroidTest